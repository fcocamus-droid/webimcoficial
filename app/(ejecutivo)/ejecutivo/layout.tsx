import PanelLayout from '@/app/components/panel/PanelLayout'

export default function EjecutivoInnerLayout({ children }: { children: React.ReactNode }) {
  return <PanelLayout>{children}</PanelLayout>
}
